// Titles and chat suggestions are cheap, fixed side tasks — not part of the chat reply, so the
// teacher's picked model doesn't apply to either. Fixed also keeps suggestions fast.
export const SIDE_TASK_MODEL = "gpt-4o-mini";
