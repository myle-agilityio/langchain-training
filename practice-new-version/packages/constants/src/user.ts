// Browser-generated id (see apps/web's useUserId store), forwarded to the agent so /api/threads
// can scope each visitor's thread list to their own browser.
export const USER_ID_HEADER = "x-inbox-user-id";
