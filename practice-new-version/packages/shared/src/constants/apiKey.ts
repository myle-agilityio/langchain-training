// BYOK: header carrying the visitor's OpenAI key, forwarded frontend -> agent HTTP routes and
// to the graph as copilotkit_forwarded_headers.
export const OPENAI_API_KEY_HEADER = "x-openai-api-key";
