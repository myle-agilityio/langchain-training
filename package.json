{
  "name": "langchain-training",
  "private": true,
  "description": "Repo-level tooling (git hooks, commit linting) shared across all project folders",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky"
  }
}
